// Notacion
// ;       delimitar el final de una linea.
const b = 4;

b + 4;


// .       se utiliza en los objetos para acceder a los atributos.
// movil.anchura movil.altura movil.modelo etc



// []      listas, arreglos o arrays como quieras llamarlos.
const ar = [1, 2, 3, 4]
console.log (ar[2])


// ()      funciones.
function suma(a, b){
    // se escribe la funcion
}


// {}      Llaves para objetos, funciones y estructuras de control.
const movil = {
    anchura: 5,
    altura: 10
}

if (true) {
     //  todo lo que alla entre llaves

     const constante2 = "hola"
}